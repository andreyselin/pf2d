{
  "name": "pf2d",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-for-test": "rm -rf dist && tsc",
    "b": "rm -rf dist && mkdir dist && npm run copy-public && webpack",
    "w": "webpack"
  },
  "author": "Andrey Selin",
  "license": "ISC",
  "dependencies": {
    "ts-loader": "^9.2.5",
    "typescript": "^4.4.2",
    "webpack": "^5.52.1"
  },
  "devDependencies": {
    "webpack-cli": "^4.8.0"
  }
}
